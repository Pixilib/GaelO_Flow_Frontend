import { BsInfoCircle } from "react-icons/bs";

const Info = (props) => {
    return (
        <BsInfoCircle 
            {...props} 
            className="text-white dark:text-white"
        />
    );
}

export default Info;
