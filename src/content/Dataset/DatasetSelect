// import React, { useEffect, useState } from 'react'
// import Select from 'react-select/creatable'

// type DatasetSelectProps = {
//   // List of options to display
//    // onChange:  // Callback function when selection changes
//     closeMenuOnSelect?: boolean;  // Optional prop to control menu behavior
// //}

// //const DatasetSelect = ({ , onChange, closeMenuOnSelect = true }: DatasetSelectProps) => {

//     const [selectedModalities, setSelectedModalities] = useState<any[]>([])  // State to keep track of selected options


//     // Function to format each option label
//     const formatOptionLabel = ({ label }: any): JSX.Element => (
//         <div className='flex'>
//             <div>{label}</div>
//         </div>
//     )

//     // Function to handle changes in selected options
//     const changeListener = (options: any) => {
//         if (options === null) options = []
//         const selectedModalities = options.map((option: any) => option.value)
//         onChange(selectedModalities)
//     }

//     return (
//         <Select
//             isMulti
//             menuPosition="fixed"
//             options={modalities.map(modality => ({ value: modality, label: modality }))}
//             formatOptionLabel={formatOptionLabel}
//             value={selectedModalities}
//             onChange={changeListener}
//             closeMenuOnSelect={closeMenuOnSelect}
//         />
//     )
// }

// export default DatasetSelect
